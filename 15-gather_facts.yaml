- name: gather facts
  hosts: web
  become: yes
  # ansible collects the info of target server, before it executes any other fact
  tasks:
  - name: facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"