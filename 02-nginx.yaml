- name :  nginx servers
  hosts:  web
  become: yes
  tasks:
    - name: install nginx 
      ansible.builtin.dnf:
          name: nginx
          state: absent
    - name: Start service nginx, if not started
      ansible.builtin.service:
        name: nginx
        state: stopped 
        enabled: no
  
